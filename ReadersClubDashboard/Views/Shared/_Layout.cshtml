<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title>Readers Club Dashboard</title>
    <link rel="stylesheet" href="~/css/site.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@700&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            display: flex;
            min-height: 100vh;
            direction: rtl;
            font-family: "Cairo", sans-serif;
            font-optical-sizing: auto;
            font-weight: 700;
            font-style: normal;
            background-color: #f8f9fa;
        }

        .sidebar {
            border-top-left-radius:30px;
            border-bottom-left-radius: 30px;
            width: 285px;
            background-color: #343a40;
            padding-top: 20px;
            color: white;
            position: fixed;
            height: 100%;
            box-shadow: 2px 0 5px rgba(0, 0, 0, 0.1);
        }

            .sidebar h4 {
                margin-bottom: 30px;
                font-size: 1.5rem;
                font-weight:bold;
            }
            .sidebar-links{
                margin-top:20px;
            }

            .sidebar a {
                font-size:18px;
                text-align : center;
                color: white;
                text-decoration: none;
                display: block;
                padding: 30px 20px;
                transition: background-color 0.3s, padding-left 0.3s;
            }

        .sidebar a:hover {
            background-color: #495057;
            padding-left: 30px;
        }

        .main-content {
            margin-right: 290px;
            padding: 20px;
            flex: 1;
		
        }
        .main-content-page{
            padding:25px 0px;
        }

        .topbar {
            background-color: white;
            padding: 15px 20px;
            border-bottom: 1px solid #dee2e6;
            margin-bottom: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

            .topbar h5 {
                margin: 0;
                font-size: 25px;
                font-family: "Cairo", sans-serif;
                font-optical-sizing: auto;
                font-weight: 700;
                font-style: normal;
            }

        .btn-logout {
            background-color: #dc3545;
            color: white;
            border: none;
            padding: 5px 10px;
            border-radius: 3px;
            text-decoration: none;
            transition: background-color 0.3s;
        }

            .btn-logout:hover {
                background-color: #c82333;
            }
    </style>
</head>
<body>

    <!-- Sidebar -->
    <div class="sidebar">
        <h4 class="text-center">نادي القراء</h4>
        <div class="Sidebar-links">
            <a href="@Url.Action("Index", "Dashboard")">لوحة التحكم</a>
            <a href="@Url.Action("AddStory", "Story")">إضافة رواية</a>
            <a href="@Url.Action("Stories", "Story")">قائمة الروايات</a>
            <a href="@Url.Action("Index", "Category")">التصنيفات</a>
            <a href="@Url.Action("Index", "Reviews")">التقييمات</a>
            <a href="@Url.Action("Users", "Dashboard")">المستخدمين</a>
            <a href="@Url.Action("Index", "Channel")">القنوات</a>
        </div>
    </div>

    <!-- Main content -->
    <div class="main-content">
        <div class="topbar">
            <h5>نادي القراء</h5>
            <a href="#" class="btn-logout">تسجيل خروج</a>
        </div>
        <div class="main-content-page">
            <!-- مكان عرض الصفحات -->
            @RenderBody()
        </div>
    </div>

</body>
</html>
